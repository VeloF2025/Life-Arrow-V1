// Export components
export { AppointmentManagement } from './components/AppointmentManagement';
export { AppointmentManagementPage } from './pages/AppointmentManagementPage';

// Export hooks
export { useAppointments } from './hooks/useAppointments';

// Export services
export { appointmentService } from './api/appointmentService';

// Export types
// Note: Types should ideally be in a separate types.ts file within the feature
